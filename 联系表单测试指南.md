# 📧 联系表单测试指南

## ✅ 已修复的问题

1. ✅ **表单验证规则**：适配中文，主题 ≥ 2 字符
2. ✅ **邮件发送**：配置正确的 Gmail 邮箱
3. ✅ **Resend API**：测试通过，邮件发送成功

---

## 🧪 测试步骤

### 1. 访问联系页面

在浏览器打开：
```
http://localhost:3000/contact
```

### 2. 滚动到"发送消息"表单

### 3. 填写测试信息

```
姓名：曾田力
邮箱：test@example.com
主题：测试
消息：这是一条测试消息
```

**注意**：
- ✅ 主题 "测试" (2个字符) 现在**可以通过**验证
- ✅ 消息至少 5 个字符

### 4. 点击"发送消息"按钮

### 5. 查看结果

**成功提示**：
```
✅ 消息发送成功
感谢您的留言，我会尽快回复！
```

**如果失败**：
- 检查浏览器控制台
- 检查服务器日志
- 验证 `.env.local` 配置

### 6. 检查邮箱

登录 Gmail：**zengtianli1@gmail.com**

你应该会收到一封邮件：
- 标题：**[网站留言] 测试**
- 发件人：Portfolio Contact Form
- 内容包含：姓名、邮箱、主题、消息

💡 **提示**：如果在主要邮件中找不到，检查"垃圾邮件"或"社交"标签

---

## 🔍 故障排查

### 问题：表单提交后没有反应

**检查**：
1. 打开浏览器开发者工具（F12）
2. 切换到 Console 标签
3. 查看是否有错误信息

### 问题：显示"邮件服务未配置"

**原因**：`.env.local` 中缺少 `RESEND_API_KEY`

**解决**：
```bash
# 检查环境变量
cat .env.local

# 应该包含：
RESEND_API_KEY=re_bbznNopa_CSyURHqvSobRA38tasEB2UFM
CONTACT_EMAIL=zengtianli1@gmail.com
```

### 问题：提交后显示验证错误

**检查字段长度**：
- 姓名：≥ 2 字符
- 主题：≥ 2 字符
- 消息：≥ 5 字符
- 邮箱：有效的邮箱格式

### 问题：邮件发送失败

**检查 API Key**：
```bash
# 测试 API Key 是否有效
curl -X POST 'https://api.resend.com/emails' \
  -H 'Authorization: Bearer re_bbznNopa_CSyURHqvSobRA38tasEB2UFM' \
  -H 'Content-Type: application/json' \
  -d '{
    "from": "onboarding@resend.dev",
    "to": "zengtianli1@gmail.com",
    "subject": "Test",
    "html": "<p>Test</p>"
  }'
```

---

## 📊 Resend 免费账户限制

### 当前限制

- ✅ **发送额度**：100 封/天
- ⚠️ **接收地址**：只能发送到注册邮箱（zengtianli1@gmail.com）
- ⚠️ **发件地址**：只能使用 `onboarding@resend.dev`

### 如何解除限制？

**验证自定义域名**（推荐用于生产环境）：

1. 登录 Resend Dashboard
2. 进入 Domains 页面
3. 添加你的域名（如 `tianlizeng.cloud`）
4. 添加 DNS 记录：
   - MX 记录
   - TXT 记录（SPF）
   - CNAME 记录（DKIM）
5. 等待验证（通常几分钟）
6. 验证后可以：
   - 发送到任意邮箱
   - 使用自定义发件地址（如 `noreply@tianlizeng.cloud`）

---

## 🎯 预期结果

✅ **表单提交成功**  
✅ **邮件发送成功**  
✅ **Gmail 收到通知**  
✅ **可以直接回复**  

---

## 📝 下一步

1. **测试表单功能**
2. **配置 Vercel 环境变量**（部署后）
3. **考虑验证自定义域名**（可选）

---

*最后更新：2025年1月10日*

