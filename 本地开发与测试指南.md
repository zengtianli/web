# 本地开发与测试指南

## 🎯 避免部署失败的最佳实践

### ⚠️ 重要原则

**在推送代码到 GitHub 之前，务必先在本地测试构建！**

这样可以在本地发现并修复问题，避免 Vercel 部署失败。

---

## 🚀 本地测试流程

### 1. 启动开发服务器

```bash
cd /Users/tianli/Downloads/portfolio

# 启动开发服务器
pnpm run dev
```

在浏览器中访问 `http://localhost:3000`，检查：
- ✅ 页面是否正常显示
- ✅ 交互功能是否正常
- ✅ 没有控制台错误

### 2. 测试生产构建（重要！）

**在每次推送前运行：**

```bash
# 构建生产版本
pnpm run build
```

**如果构建成功**，会显示：
```
✓ Compiled successfully
✓ Generating static pages (24/24)
```

**如果构建失败**，会显示错误信息：
- 仔细阅读错误信息
- 根据错误提示修复问题
- 再次运行 `pnpm run build` 直到成功

### 3. 本地预览生产版本

```bash
# 启动生产版本预览
pnpm run start
```

访问 `http://localhost:3000`，验证生产版本是否正常。

---

## 📝 常见构建错误及解决方案

### 错误 1: 语法错误

**症状**：
```
Error: Expected ',', got '体育之星'
```

**原因**：中文引号与 JavaScript 字符串引号冲突

**解决**：
```typescript
// ❌ 错误
zh: "浙江省水利厅"体育之星""

// ✅ 正确
zh: '浙江省水利厅"体育之星"'
```

---

### 错误 2: 服务器组件事件处理器

**症状**：
```
Error: Event handlers cannot be passed to Client Component props
```

**原因**：服务器组件中使用了 `onClick` 等事件处理器

**解决**：将交互部分拆分为客户端组件

```typescript
// 在文件顶部添加
'use client'
```

---

### 错误 3: 数据结构不匹配

**症状**：
```
TypeError: Cannot read properties of undefined (reading 'map')
```

**原因**：组件期望的数据结构与实际数据不匹配

**解决**：
1. 检查组件代码，确认期望的数据格式
2. 确保 Markdown 文件的数据结构匹配
3. 不要随意改变数据结构

---

### 错误 4: TypeScript 类型错误

**症状**：
```
Type 'string' is not assignable to type 'number'
```

**原因**：类型不匹配

**解决**：
```bash
# 检查 TypeScript 错误
pnpm run type-check

# 或者在 VSCode 中查看红色波浪线
```

---

## 🔧 完整的开发工作流

### 标准流程（推荐）

```bash
# 1. 拉取最新代码
git pull origin main

# 2. 安装依赖（如果有更新）
pnpm install

# 3. 启动开发服务器
pnpm run dev

# 4. 进行修改...

# 5. 测试构建（关键步骤！）
pnpm run build

# 6. 如果构建成功，提交代码
git add -A
git commit -m "feat: 描述你的修改"

# 7. 推送到 GitHub
git push origin main
```

---

## 📋 提交前检查清单

在推送代码前，确认：

- [ ] ✅ 代码没有语法错误
- [ ] ✅ `pnpm run build` 构建成功
- [ ] ✅ 本地开发服务器正常运行
- [ ] ✅ 页面显示正确
- [ ] ✅ 没有控制台错误
- [ ] ✅ TypeScript 类型检查通过
- [ ] ✅ 修改的内容已测试

---

## 🛠️ 有用的命令

### 开发相关

```bash
# 启动开发服务器（热重载）
pnpm run dev

# 构建生产版本
pnpm run build

# 启动生产版本预览
pnpm run start

# 清理缓存（如果遇到奇怪问题）
rm -rf .next
pnpm run build
```

### 代码检查

```bash
# 检查 TypeScript 类型（如果配置了）
pnpm run type-check

# 检查代码风格（如果配置了）
pnpm run lint

# 修复代码风格问题
pnpm run lint:fix
```

### Git 操作

```bash
# 查看修改状态
git status

# 查看具体修改
git diff

# 撤销未提交的修改
git checkout -- <file>

# 撤销最后一次提交（保留修改）
git reset --soft HEAD~1
```

---

## 🎯 最佳实践总结

### ✅ 应该做的

1. **始终先在本地测试构建**
2. **修改数据文件前先查看组件代码**
3. **一次只修改一个功能**
4. **写清晰的 commit 信息**
5. **遇到错误先仔细阅读错误信息**
6. **保存重要文件的备份**

### ❌ 不应该做的

1. **不要直接修改就推送**
2. **不要随意改变数据结构**
3. **不要忽略构建错误**
4. **不要在服务器组件中使用事件处理器**
5. **不要使用中文引号包裹含有中文引号的字符串**

---

## 🆘 遇到问题怎么办

### 1. 查看错误信息

仔细阅读终端中的错误信息，通常会告诉你：
- 哪个文件有问题
- 第几行有问题
- 什么类型的错误

### 2. 搜索错误

复制错误信息到 Google/ChatGPT 搜索，通常能找到解决方案。

### 3. 回滚到上一个工作版本

```bash
# 查看提交历史
git log

# 回滚到指定提交
git reset --hard <commit-hash>

# 强制推送（谨慎使用）
git push -f origin main
```

### 4. 检查 Vercel 部署日志

访问 Vercel Dashboard，查看详细的部署日志，找出具体错误。

---

## 📚 参考资源

- [Next.js 文档](https://nextjs.org/docs)
- [React 服务器组件](https://nextjs.org/docs/app/building-your-application/rendering/server-components)
- [TypeScript 手册](https://www.typescriptlang.org/docs/)
- [Git 基础](https://git-scm.com/book/zh/v2)

---

## 🎉 总结

记住这个黄金法则：

> **在推送前，先本地 `pnpm run build`！**

这样可以避免 99% 的部署失败问题。

---

**创建时间**: 2025年10月11日  
**最后更新**: 2025年10月11日

