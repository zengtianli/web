# 🧪 快速测试联系表单

## 测试 1：诊断环境变量

```bash
# 访问诊断端点
curl https://tianlizeng.cloud/api/debug
```

**预期结果**：
- `"exists": true` for both keys
- `"valid": true` for RESEND_API_KEY
- `"isGmail": true` for CONTACT_EMAIL

---

## 测试 2：提交表单

```bash
curl -X POST https://tianlizeng.cloud/api/contact \
  -H "Content-Type: application/json" \
  -d '{
    "name": "测试",
    "email": "test@example.com",
    "subject": "测试",
    "message": "这是测试"
  }'
```

**成功响应** (200)：
```json
{"success":true,"message":"消息发送成功"}
```

**失败响应**：
- `500` + "邮件服务未配置" → 环境变量未配置
- `429` + "请求过于频繁" → Rate limiting
- `403` + "Unauthorized" → API Key 无效
- `400` + "数据格式不正确" → 表单验证失败

---

## 测试 3：检查邮箱

登录 Gmail：`zengtianli1@gmail.com`

查找邮件：
- 主题：[网站留言] 测试
- 发件人：Portfolio Contact Form

💡 提示：可能在垃圾邮件中

---

## 测试 4：查看 Vercel 日志

1. 进入 Vercel Dashboard
2. 项目 → Functions 标签
3. 找到 `/api/contact`
4. 查看最近的调用记录
5. 点击查看详细日志

---

## 常见错误对照表

| 错误信息 | 原因 | 解决方案 |
|---------|------|---------|
| "邮件服务未配置" | RESEND_API_KEY 未设置 | 添加环境变量并重新部署 |
| "Unauthorized" | API Key 无效 | 检查 API Key 是否正确 |
| "You can only send to..." | 收件地址限制 | 使用 Gmail 邮箱或验证域名 |
| "请求过于频繁" | Rate limiting | 等待 1 小时后重试 |
| 超时 | Resend API 响应慢 | 检查网络或稍后重试 |

---

## 完整测试流程

```bash
# 1. 检查环境变量
curl https://tianlizeng.cloud/api/debug

# 2. 提交测试表单
curl -X POST https://tianlizeng.cloud/api/contact \
  -H "Content-Type: application/json" \
  -d '{"name":"测试","email":"test@example.com","subject":"测试","message":"这是测试消息"}'

# 3. 查看响应（应该是 200 + success: true）

# 4. 登录 Gmail 检查邮件
```

---

## ✅ 成功标志

1. `/api/debug` 显示所有配置正确
2. 表单提交返回 200 状态码
3. Gmail 收到邮件通知
4. 可以回复邮件（reply-to 地址正确）

如果以上都成功，说明联系表单功能完全正常！🎉

