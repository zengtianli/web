---
description: 网站部架构规范
globs: ["**/*"]
alwaysApply: true
---

# 网站部架构

## 部门定位

**路径**：`~/Downloads/personal_site/`

**职责**：
- ✅ 网站代码开发（Next.js, React）
- ✅ 前端 UI/UX 设计
- ✅ 部署运维（Vercel）
- ✅ SEO 优化

**不负责**：
- ❌ 简历内容撰写（总部负责）
- ❌ 档案材料管理（总部负责）

## 数据来源

| 内容 | 来源 | 链接方式 |
|------|------|----------|
| 简历素材 | `~/cursor-shared/personal/resume/` | 软链接 → `content/resume-source/` |
| 档案扫描件 | `~/cursor-shared/archives/` | 软链接 → `public/archives/` |

## 目录结构

```
personal_site/
├── app/                    # Next.js 页面
├── components/             # React 组件
├── content/                # 内容文件
│   ├── resume-source → ~/cursor-shared/personal/resume/  # 软链接
│   └── ...
├── public/
│   ├── archives → ~/cursor-shared/archives/  # 软链接
│   └── images/
├── lib/                    # 工具函数
└── .cursor/
    ├── rules/              # 规则（部分软链接总部）
    └── agents/             # 网站专用 agents
```

## 技术栈

- Next.js 14+
- React 18+
- TypeScript
- Tailwind CSS
- MDX

## 开发规范

1. 组件放在 `components/` 下，按原子设计分层
2. 页面放在 `app/` 下，使用 App Router
3. 内容更新先更新总部，再同步到网站
